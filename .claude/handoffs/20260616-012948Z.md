# Session handoff — 20260616-012948Z

## Branch state
- Repo: latiq (`/Users/sujeetv/hub/latiq`), remote `neonexia/latiq`
- Branch: `dataset-catalog` (**PR #27 OPEN, not merged**)
- Last commit: `78a4d8b` test(catalogs): real DuckLake e2e + iceberg/MinIO harness + CI
- Working tree: clean except untracked `.claude/handoffs/*`
- PR #27 now bundles the WHOLE datasets+catalogs feature (many commits): Slice-1 dataset catalog → reshape to catalogs/datasets split → iceberg+ducklake attachers → MCP agent tools → e2e + CI.
- **Direct push to main is BLOCKED by the safety classifier.** Always branch → `gh pr create` → `gh pr merge --merge --delete-branch`.

## What we just finished
- Full **datasets + catalogs** feature, live-verified, on PR #27. Datasets = simple files in the built-in `latiq` catalog (`load` copies in). Catalogs = external attachable sources (iceberg, ducklake) — registered with `--set` locator params, `pull`ed transiently (attach→query→detach, nothing persists).
- **Attacher abstraction** (swarm-style): `latiq-common/src/catalog.rs` (per-type allowlist of persistable params — credentials dropped at `add`); `latiq-engine-duckdb/src/attachers.rs` (iceberg + ducklake → CREATE SECRET/ATTACH SQL). Engine `pull_catalog`/`describe_catalog` in `duck_engine.rs`.
- **MCP agent tools** (`latiq-mcp/src/server.rs`): list_datasets, load_dataset, list_catalogs, describe_catalog, pull_catalog — rich docstrings-as-contract + `latiq://recipes/external-data` resource + handshake instructions (trelisdb-style guidance).
- **e2e + CI**: `tests/catalogs.rs` (deterministic DuckLake pull, in suite); `tests/catalogs_iceberg.rs` (#[ignore]d, env-driven) + `deploy/iceberg-minio/` docker harness; `.github/workflows/ci.yml` (NEW — fmt+clippy+test, opt-in iceberg job).

## What's next (in order)
1. **Merge PR #27** (user kept saying "merge it" then redirecting — confirm then `gh pr merge 27 --merge --delete-branch`). 18 commits.
2. **Iceberg e2e is UNVERIFIED** — Docker daemon was down all session. When Docker is up: `deploy/iceberg-minio/up.sh` then the `LATIQ_ICEBERG_* / LATIQ_S3_* cargo test -p latiq --test catalogs_iceberg -- --ignored`. The compose/seed (`apache/iceberg-rest-fixture` + minio + pyiceberg) may need version pinning — noted in `deploy/iceberg-minio/README.md`.
3. **Catalog pull/describe forwarding** — AgentOps `prepare_pull` (ops.rs ~line 360) ERRORS if the pond is owned by another node ("must be sent to the pond's owning node"). The CLI hits the owner directly so it works; the MCP/gateway multi-node path needs a Forwarder method. Small follow-up.
4. **`Closes #24` in commit `4eeba25` is wrong** (#24 = extensions, already closed by #25). Harmless — datasets/catalogs had no tracking issue.
5. Backlog issues: **#23** (k8s), **#26** (credentials — pass-through token at load, do with #5 identity), **#6** (error handling), **#5** (OIDC). **#24** done.

## Mental-model traps from this session
- **catalog vs dataset are SEPARATE first-class concepts — do not merge them.** User corrected this ~4x. catalog = external attachable DB (iceberg/ducklake), tables discovered; dataset = simple file(s) in the built-in `latiq` catalog. NO namespaces (dropped). See `docs/dataset.md`.
- **External catalogs are PULL-ONLY and transient. Never queried live, never persistently attached.** `attach → run query → detach` is one atomic op. This killed the persistent-attach + stored-creds + re-attach-on-restart complexity. (Earlier I built `pond_attachments` table + persistent attach + the namespace model — all REMOVED.)
- **Latiq stores ZERO credentials.** The per-type allowlist (`catalog::filter_params`) DROPS credential-shaped keys (`token`, `s3_secret_key`) at `add`; creds ride in at pull/describe via `--set`, merged (pull wins), into a TEMPORARY DuckDB secret, dropped on detach. Verified: CLI prints "(not stored, pass at pull: token)".
- **Unified `--set key=value` on add/pull/describe** (no special `--token` flag). The attacher maps keys→SQL. User insisted on consistency.
- **ATTACH cannot run inside a transaction** → can't reuse `write_query` (it wraps BEGIN/COMMIT). That's why there are dedicated engine `pull_catalog`/`describe_catalog` methods.
- **DuckDB iceberg/ducklake/httpfs have NO duckdb-rs cargo feature** (only json/parquet do). So they're loaded at runtime from the baked image, never statically linked. (`Cargo.toml` duckdb features = `bundled, json, parquet`.)
- **`hold on / discuss` means STOP and design, don't implement.** User repeatedly wanted discussion before code on this feature. They dislike jargon ("too much jargon, keep it simple").
- Migration v4 was rewritten 3x in-place (it's unmerged so editable; append-only rule only applies to SHIPPED migrations).

## Decisions made this session, not yet in code or docs
- **Multi-table datasets → schema-per-dataset was discussed but NOT implemented** — load currently creates flat tables (table per dataset_table, by the table's own name). `docs/dataset.md` implies schema; reconcile if it matters.
- **k8s/extensions notes** are in memory (`k8s-tiers-use-full-pod.md` → issue #23) — don't re-derive.
- CLAUDE.md still says "no CI workflow, per direction" — but we ADDED `.github/workflows/ci.yml` this session (user asked for CI). CLAUDE.md note is now stale.

## Open threads
- PR #27 merge pending user confirm.
- Iceberg-real verification pending Docker.
- Forwarding for catalog pull (multi-node MCP) deferred.
- Credentials-from-agent-identity (the `set` token → caller bearer) deferred to #26/#5.

## Recovery pointers
- Read `docs/dataset.md` FIRST for the catalog/dataset model + credential rules.
- `crates/latiq-common/src/catalog.rs` (allowlist), `crates/latiq-engine-duckdb/src/attachers.rs` (per-type SQL), `crates/latiq-engine-duckdb/src/duck_engine.rs` `pull_catalog`/`describe_catalog`.
- `crates/latiq-mcp/src/server.rs` (5 new tools) + `resources.rs` (`latiq://recipes/external-data`).
- `crates/latiq/tests/catalogs.rs` (the working e2e pattern) + `deploy/iceberg-minio/README.md`.
- Reference repos: `~/hub/swarm/src/swarm/catalog/attachers.py` (attacher pattern), `~/hub/trelisdb/crates/trelisdb-mcp/` (MCP guidance pattern).
- Gate before any PR: `cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace` (all green now).

## Active plan / todos
No plan file. No active TaskList. State: clean `dataset-catalog` branch, PR #27 ready to merge.
