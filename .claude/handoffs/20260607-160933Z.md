# Session handoff — 20260607-160933Z

## Branch state
- Repo: latiq (`/Users/sujeetv/hub/latiq`), remote `neonexia/latiq` (private)
- Branch: `slice0-m3plus`
- Last commit: `fa9933e` fix(m11a): harden the engine read/write/explain guards
- Working tree: **clean** (all pushed)
- Ahead of `main`: 32 commits. `main` is the M1+M2 baseline; all M3–M11 work is on this branch and pushed.

## What we just finished
- **M11a (`fa9933e`)**: fixed the engine guard cluster in `crates/latiq-engine-duckdb/src/exec.rs` with edge tests — `run_write` now executes user SQL as its OWN statement (not a `BEGIN;…;COMMIT;` string), attribution `set_commit_message` issued LAST, ROLLBACK on error (fixes trailing-comment wedge + forgery + dangling-txn); SELECT-via-write_query creates no snapshot; `is_read_only` word-scans for write keywords (catches `WITH…INSERT`); `run_explain` refuses `EXPLAIN ANALYZE`; `cell_to_json` HugeInt→string when > i64.
- **M10 (`1425be2`)**: MCP agent surface completed — `crates/latiq-mcp/src/resources.rs` (latiq:// resources + 4 prompt SOPs), tool annotations + mini-tutorial descriptions in `server.rs`, `latiq-client` got list_tools/list_resource_uris/read_resource_text/list_prompt_names/get_prompt_text. Tests: `crates/latiq/tests/mcp.rs` (5 pass).
- **M9 (`df8ff18`)**: CLI rewired to gRPC (data→pond node Data gRPC, metadata/admin→control-plane Admin gRPC). Added Admin `PondList`. `crates/latiq/tests/admin.rs`.
- **M8 (`50b25ae`)**: Data/Query gRPC surface (`crates/latiq-pond-node/src/data_service.rs`, proto `data.proto`) + full-stack harness `crates/latiq/tests/common/mod.rs` + `query_grpc.rs` (8 pass).

## What's next (in order) — finishing M11b
I was MID-EDIT adding `forget_pond` to the engine (interrupted before editing). Continue:
1. **Engine instance eviction (review #11, leak).** Add `fn forget_pond(&self, loc: &PondLocation);` to `QueryEngine` trait (`crates/latiq-engine/src/engine.rs` after `describe_schema` at line 47). Impl in `DuckEngine` (`crates/latiq-engine-duckdb/src/duck_engine.rs`): `self.instances.lock()...remove(&loc.catalog_uri);`. Call it in `AgentOps::drop_pond` (`crates/latiq-agent-core/src/ops.rs` ~line 123) — fetch `storage.pond_location(pid)` then `engine.forget_pond(&loc)` BEFORE `storage.drop_pond`. Small.
2. **NodeNotFound→pond_not_found mismap (review #13).** `crates/latiq-control-plane/src/control_service.rs` `to_status`: map `NodeNotFound` to `Status::failed_precondition` (NOT not_found). `crates/latiq-pond-node/src/grpc_control.rs` `status_err`: add `Code::FailedPrecondition => AgentError::internal(...)`. Edge test: allocate with no node ≠ pond_not_found. Small.
3. **list_ponds tolerate concurrent drop (review #9).** In `RegistryControlPlane::list_ponds` (`crates/latiq-agent-core/src/registry_control.rs` ~line 65) and `ControlService`/`AdminService` pond-list loops: skip rows whose `pond_info` errors (dropped mid-iteration) instead of failing the whole call. Small.
4. **Remaining review items (lower priority, note as tracked, not blocking):** drop_pond `confirm` gate still dead (review #5); `redact_sql` mangles identifiers / misses comments (#14); mutex `.expect` poisoning bricks pond/control-plane (#7 — could `lock().unwrap_or_else(|e| e.into_inner())`); drop_pond cancel TOCTOU (#10); abort-watcher post-completion interrupt race (PLAUSIBLE).

## Mental-model traps from this session
- **MCP is AGENT-ONLY. CLI/SDK are NOT agents → they use gRPC, never MCP.** I kept building the CLI as an MCP client; user corrected hard. Data ops → Data gRPC (pond node); metadata reads + admin → Admin gRPC (control plane). `latiq-client` (MCP client) is agent-sim/tests ONLY. → CLAUDE.md invariants 1–2, spec §0 addendum.
- **Control plane is NEVER in the query path.** Queries run on the pond node only. Metadata reads (pond list/describe) come from the control plane so they work when pond nodes are down. → CLAUDE.md invariants 3–4. (`pond list` with pond-node killed is the verified proof.)
- **`latiq-agent-core` is PROTOCOL-NEUTRAL.** No rmcp/tonic/http types in it. New surfaces = new inbound adapters onto `AgentOps` (MCP, Data gRPC), never core changes. → CLAUDE.md invariant 5, `crates/latiq-agent-core/CLAUDE.md`.
- **One DuckDB instance per pond (mutex-guarded, reused).** Instance-per-query loses concurrent writes (we hit this in M7). → invariant 7, `crates/latiq-engine-duckdb/CLAUDE.md`.
- **Don't test DuckDB; test our integration.** Cancellation/resource-release, concurrency, guards, cell conversion, attribution plumbing — not DuckDB SQL semantics. → invariant 10. (User said this explicitly re M11 tests.)
- **"Bug we caused → just fix it, don't frame as a regression test."** Add feature tests (happy + edge) per feature; those become the regression net. → user instruction this session.
- **Subagent dispatches drop on long runs (socket errors).** Implement directly in-session for anything heavy; subagents OK only for short read-only tasks. Pre-warm `duckdb` bundled compile (background `cargo build`) before any dispatch that builds it.

## Decisions made this session, not yet fully in code
- **Data gRPC is unary + JSON-encoded result (`JsonResponse.json`), bounded by inline cap.** Arrow Flight SQL streaming stays M2. (Implemented in M8; just don't "upgrade" to streaming.)
- **M11b in progress** (items 1–3 above). Everything else from the `/code-review max` run (15 findings) is triaged in the handoff's "remaining" list — fix opportunistically, none block.
- Test taxonomy is real and must be kept: e2e files per surface (`tests/mcp.rs`/`query_grpc.rs`/`admin.rs`), fn names prefixed by feature (`pond_lifecycle_*`, `sql_read_write_*`, …). `cargo test <feature>` targets across surfaces. → CLAUDE.md "Test taxonomy".

## Open threads
- User is driving milestone-by-milestone; finish M11b then likely ask what's next (federation/catalogs is the natural next slice, deferred).
- The `/code-review max` findings list (15) was delivered in-conversation; M11a fixed the engine cluster. The rest are in "What's next" item 4.
- No PR opened to `main` yet (user hasn't asked; I held off). Branch is pushed.

## Recovery pointers (read first)
1. `CLAUDE.md` (root) — the 12 design invariants + 3 surfaces + test taxonomy. Non-negotiable.
2. `docs/superpowers/specs/2026-06-04-latiq-slice0-design.md` §0 addendum — the reshaping (M8–M11).
3. Per-crate `crates/*/CLAUDE.md` (agent-core, mcp, engine-duckdb, control-plane, pond-node).
4. `crates/latiq/tests/common/mod.rs` — the full-stack harness `start_stack()` (control-plane + pond-node in-process; exposes data/admin/mcp/control endpoints).
5. `docs/usage.md` — current run instructions (`./dev.sh`, gRPC CLI).

## Active plan / todos
No formal plan file for M8–M11 (implemented directly). Status: **M1–M10 complete + pushed; M11a complete + pushed; M11b items 1–3 are the immediate next work** (see "What's next"). All gates: `cargo test --workspace` green (~28 suites), `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo fmt --all`. `protoc` required to build. `dev.sh` runtime artifacts (`latiq-data/`, `latiq-cp.duckdb`) are gitignored.
