/target
/spike/target
**/*.rs.bk
.DS_Store

# Local override if you point --root at the repo (default is ~/.latiq, outside the tree).
/.latiq
/latiq-data
/latiq-cp.duckdb
/latiq-cp.duckdb.wal
/.latiq-dev
