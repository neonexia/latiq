/target
/spike/target
**/*.rs.bk
.DS_Store

# Local override if you point --root at the repo (default is ~/.latiq, outside the tree).
/.latiq
/latiq-data
/latiq-cp.duckdb
/latiq-cp.duckdb.wal
/.latiq-dev

# Prometheus TSDB if you run `prometheus` from deploy/ with the example config.
/deploy/data

# Internal working notes — kept on disk for the local agent workflow, never published.
/.claude/handoffs/
/docs/superpowers/
